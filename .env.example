# Скопируйте этот файл в .env и подставьте свой OAuth Client ID из Google Cloud.
# Файл .env в git не попадает (см. .gitignore).
VITE_GOOGLE_CLIENT_ID=YOUR_GOOGLE_CLIENT_ID.apps.googleusercontent.com
